//微信小程序、H5通用封装
const request = require('request')
let config = require('../pay/config')
const util = require("../../util/index")
config = config.mch
module.exports = {
	order(attach, body, total_fee, notify_url, ip){
		return {}
	}
}